<?xml version="1.0"?>
<ruleset name="VoipMsClientPHP">
  <description>Enforce PSR‑12 and project conventions</description>

  <!-- Base standard -->
  <rule ref="PSR12"/>

  <!-- Exclude vendor, tests, etc. -->
  <exclude-pattern>*/vendor/*</exclude-pattern>
  <exclude-pattern>*/tests/*</exclude-pattern>
</ruleset>
